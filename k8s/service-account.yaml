apiVersion: v1
kind: ServiceAccount
metadata:
  name: deepseek-ci-ksa
  namespace: default
  annotations:
    iam.gke.io/gcp-service-account: deepseek-ci@YOUR_ACTUAL_PROJECT_ID.iam.gserviceaccount.com  # UPDATE THIS WITH YOUR ACTUAL PROJECT ID